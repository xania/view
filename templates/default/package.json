{
  "name": "{{ name }}",
  "description": "{{ description }}",
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "examples/*"
  ],
  "scripts": {
    "start": "npx vite",
    "build": "npx vite build",
    "preview": "npx vite preview"
  },
  "files": [
    "dist"
  ],
  "license": "MIT",
  "dependencies": {
    "@xania/router": "latest"
  },
  "devDependencies": {
    "vite": "latest"
  }
}
