{
  "name": "{{ kebab name }}",
  "description": "{{ description }}",
  "version": "0.0.0",
  "author": "{{ author }}",
  "scripts": {
    "start": "npx vite",
    "build": "npx vite build",
    "bootstrap": "echo 'bootstrap'"
  },
  "files": [
    "dist"
  ],
  "license": "MIT",
  "dependencies": {
    "vite": "latest"
  }
}
