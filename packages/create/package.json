{
  "name": "create-xania",
  "version": "0.1.79",
  "private": false,
  "author": "Ibrahim ben Salah",
  "main": "./dist/cli.cjs",
  "types": "./dist/cli.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsup src/cli.ts --minify --format cjs --target node16",
    "clean": "npx rimraf dist",
    "dev": "tsup src/cli.ts --sourcemap --format cjs --target node16 --watch",
    "prepublishOnly": "npm run clean && npm version patch && npm run build",
    "test": "npx rimraf test-app && node dist/cli.cjs test-app"
  },
  "bin": {
    "xania": "dist/cli.cjs"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@types/degit": "^2.8.3",
    "@types/node": "^17.0.29",
    "prettier": "^2.8.1",
    "tsup": "^5.12.1",
    "typescript": "^4.6.3"
  },
  "license": "MIT",
  "dependencies": {
    "degit": "^2.8.4",
    "enquirer": "^2.3.6",
    "execa": "^7.0.0",
    "rimraf": "^3.0.2"
  }
}
