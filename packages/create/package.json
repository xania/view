{
  "name": "create-xania",
  "version": "0.1.95",
  "private": false,
  "author": "Ibrahim ben Salah",
  "main": "./dist/cli.cjs",
  "types": "./dist/cli.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsup src/cli.ts --minify --format cjs --target node16",
    "clean": "npx rimraf dist",
    "dev": "tsup src/cli.ts --sourcemap --format cjs --target node16 --watch",
    "prepublish": "npm run clean && npm version patch && npm run build",
    "test": "npx rimraf my-project && node dist/cli.cjs test-app"
  },
  "bin": {
    "xania": "dist/cli.cjs"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@types/node": "^17.0.45",
    "prettier": "^2.8.8",
    "replace-in-file": "^6.3.5",
    "tsup": "^5.12.9",
    "typescript": "^4.9.5",
    "tar": "^6.1.14",
    "chalk": "^5.2.0",
    "sander": "^0.6.0",
    "home-or-tmp": "^4.0.0",
    "https-proxy-agent": "^6.1.0"
  },
  "license": "MIT",
  "dependencies": {
    "enquirer": "^2.3.6",
    "execa": "^7.1.1",
    "rimraf": "^3.0.2"
  }
}
