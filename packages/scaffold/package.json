{
  "name": "@xania/scaffold",
  "version": "0.0.21",
  "private": false,
  "author": "Ibrahim ben Salah",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/cli.ts --minify --sourcemap --format esm --dts",
    "clean": "npx rimraf dist",
    "dev": "tsup src/cli.ts --watch --sourcemap --format esm --dts",
    "prepublishOnly": "npm run clean && npm version patch && npm run build"
  },
  "files": [
    "dist",
    "bin"
  ],
  "devDependencies": {
    "@types/node": "^17.0.29",
    "prettier": "^2.8.1",
    "rimraf": "^3.0.2",
    "tsup": "^5.12.1",
    "typescript": "^4.6.3"
  },
  "license": "MIT",
  "dependencies": {
    "@types/degit": "^2.8.3",
    "degit": "^2.8.4",
    "enquirer": "^2.3.6",
    "execa": "^7.0.0",
    "replace-in-file": "^6.3.5",
    "undefined": "^0.1.0"
  }
}
